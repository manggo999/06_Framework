package edu.kh.demo.controller;

import org.springframework.stereotype.Controller;

@Controller // 요청과 응답을 제어하는 역할 명시 + Bean(==Spring이 만들고 관리하는 객체)등록
public class ExampleController {
	
	/*
	 *  1) @RequestMapping("주소")
	 * 
	 * 	2) @GetMapping("주소")    :  Get(조회) 방식 요청 매핑
	 * 
	 *     @PostMapping("주소")	  :  Post(삽입) 방식 요청 매핑
	 *     
	 *     @PutMapping("주소")    :  Put(수정) 방식 요청 매핑(form, a태그 요청 불가)
	 * 
	 * 	   @DeleteMapping("주소") :  Delete 방식 요청 매핑(form, a태그 요청 불가)
	 * */

}
